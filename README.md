# ujwald36.github.io

Personal website for Ujwal Dinesha — Ph.D. candidate at Texas A&M University.

Plain HTML + CSS, hosted on GitHub Pages. No build step.

## Structure

- `index.html` — page content
- `style.css` — styles
- `Ujwal.jpg` — portrait

## Local preview

```bash
python -m http.server 8000
```

Then open `http://localhost:8000`.
